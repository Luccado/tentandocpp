#include <iostream>
#include <cmath>

int main() {


    double x = 3.99;
    double y = 4;
    double z;

    //z = std::max(x, y);  maior valor
    //z = std::min(x, y);  menor valor
    //z = pow(2, 3);  elevado
    //z = sqrt(9);  raiz quadrada
    //z = abs(-1234); valor absoluto
    //z = round(x);
    //z = ceil(x); arredondar para cima
    //z = floor(x); arredondar para baixo

    std::cout << z;


    return 0;
}